---
description: Gradle 빌드 실행 및 에러 수정
---

# Build

Spring Boot 프로젝트 빌드 및 에러 해결:

1. 빌드 실행: `./gradlew build`

2. 에러 발생 시:
   - 컴파일 에러: 파일별로 그룹화하여 수정
   - 테스트 실패: 실패한 테스트 분석 후 수정
   - 의존성 문제: build.gradle.kts 확인

3. 수정 후 재빌드하여 검증

4. 3회 시도 후에도 동일 에러 시 사용자에게 보고

빌드 성공 시 결과 요약 출력.
